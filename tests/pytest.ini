[pytest]
# The package lives at the repository root (package-dir maps hydro_tools -> .),
# so the root has an __init__.py. Keeping this ini inside tests/ makes tests/
# the rootdir, which stops pytest from collecting the root __init__.py as a
# stray package. importlib mode imports each test module by path.
addopts = --import-mode=importlib
